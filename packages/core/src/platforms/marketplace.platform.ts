/**
 * Universal Marketplace Platform Adapter (Backward Compatibility Re-export)
 */
import { UniversalPlatform } from './universal.platform';

export const MarketplacePlatform = UniversalPlatform;
export type MarketplacePlatform = UniversalPlatform;
